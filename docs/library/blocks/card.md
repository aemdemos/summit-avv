# Card

## What it is
A single card item intended to be used inside card collections (for example `Cards`).

## Authoring (table shape)
- Varies by parent block/model; typically a row representing a single card with image + text + link fields.

## Implementation
- **Code**: `blocks/card/card.js`
- **Styles**: inherited / block-level (see parent block styles)

