# Modal

## What it is
A reusable modal/dialog block, often opened from links to `/modals/` paths.

## Authoring
Typically authored as a separate “modal page” and opened via a link.

## Implementation
- **Code**: `blocks/modal/modal.js`
- **Styles**: `blocks/modal/modal.css`

